import React, { useState, useEffect } from 'react'; import { BrowserRouter as Router, Routes, Route } from 'react-router-dom'; import Navbar from './components/Navbar'; import HomePage from './pages/HomePage'; import SearchPage from './pages/SearchPage'; import MovieDetailPage from './pages/MovieDetailPage'; import WatchlistPage from './pages/WatchlistPage'; import RecommendationsPage from './pages/RecommendationsPage'; function App() { const [userId, setUserId] = useState(null); useEffect(() => {let storedUserId = localStorage.getItem('userId'); if (!storedUserId) {storedUserId = Math.floor(Math.random() * 1000000).toString(); localStorage.setItem('userId', storedUserId);} setUserId(storedUserId);}, []); if (!userId) return (<div className='flex items-center justify-center min-h-screen bg-gradient-to-br from-slate-900 to-slate-800'><div className='animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-primary'></div></div>); return (<Router><div className='min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900'><Navbar /><Routes><Route path='/' element={<HomePage userId={userId} />} /><Route path='/search' element={<SearchPage userId={userId} />} /><Route path='/movie/:id' element={<MovieDetailPage userId={userId} />} /><Route path='/watchlist' element={<WatchlistPage userId={userId} />} /><Route path='/recommendations' element={<RecommendationsPage userId={userId} />} /></Routes></div></Router>);} export default App;