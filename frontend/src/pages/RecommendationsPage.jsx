import React, { useState, useEffect } from 'react'; import { motion } from 'framer-motion'; import MovieCard from '../components/MovieCard'; import { getRecommendations } from '../services/api'; function RecommendationsPage({ userId }) { const [recommendations, setRecommendations] = useState([]); const [loading, setLoading] = useState(true); useEffect(() => {fetchRecommendations();}, [userId]); const fetchRecommendations = async () => {setLoading(true); try {const response = await getRecommendations(userId, 15); setRecommendations(response.data.data);} catch (error) {console.error('Error:', error);} finally {setLoading(false);}}; return (<div className='min-h-screen py-8 px-4 sm:px-6 lg:px-8'><div className='max-w-7xl mx-auto'><motion.div initial={{opacity: 0, y: -20}} animate={{opacity: 1, y: 0}} className='mb-12'><h1 className='text-4xl font-bold text-white mb-4'>Recommendations <span className='text-primary'>For You</span></h1><p className='text-slate-400'>Based on your ratings and watchlist history</p></motion.div>{loading ? (<div className='flex items-center justify-center min-h-96'><div className='animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-primary'></div></div>) : recommendations.length > 0 ? (<motion.div initial={{opacity: 0}} animate={{opacity: 1}} className='grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-6'>{recommendations.map((movie, index) => (<motion.div key={movie.id} initial={{opacity: 0, y: 20}} animate={{opacity: 1, y: 0}} transition={{delay: index * 0.05}}><MovieCard movie={movie} userId={userId} onWatchlistAdd={fetchRecommendations} /></motion.div>))}</motion.div>) : (<div className='text-center py-12'><p className='text-slate-400 text-lg mb-4'>Rate some movies to get personalized recommendations!</p><a href='/' className='inline-block bg-primary hover:bg-red-600 text-white px-6 py-2 rounded-lg transition'>Explore Movies</a></div>)}</div></div>);} export default RecommendationsPage;