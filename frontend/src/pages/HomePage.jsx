import React, { useState, useEffect } from 'react'; import { motion } from 'framer-motion'; import MovieCard from '../components/MovieCard'; import { getMovies, getPopularMovies, getTrendingMovies } from '../services/api'; function HomePage({ userId }) { const [popular, setPopular] = useState([]); const [trending, setTrending] = useState([]); const [loading, setLoading] = useState(true); useEffect(() => {fetchMovies();}, []); const fetchMovies = async () => {try {const [popularRes, trendingRes] = await Promise.all([getPopularMovies(10), getTrendingMovies(8)]); setPopular(popularRes.data.data); setTrending(trendingRes.data.data);} catch (error) {console.error('Error:', error);} finally {setLoading(false);}}; if (loading) return (<div className='flex items-center justify-center min-h-screen'><div className='animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-primary'></div></div>); return (<div className='min-h-screen py-8 px-4 sm:px-6 lg:px-8'><div className='max-w-7xl mx-auto'><motion.div initial={{opacity: 0, y: -20}} animate={{opacity: 1, y: 0}} transition={{duration: 0.6}} className='text-center mb-12'><h1 className='text-5xl font-bold text-white mb-4'>Welcome to <span className='text-primary'>MovieFlix</span></h1><p className='text-slate-400 text-lg'>Discover movies you'll love, personalized just for you</p></motion.div><section className='mb-12'><h2 className='text-3xl font-bold text-white mb-6'>🔥 Popular Now</h2><div className='grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-6'>{popular.map((movie) => (<div key={movie.id}><MovieCard movie={movie} userId={userId} onWatchlistAdd={fetchMovies} /></div>))}</div></section><section className='mb-12'><h2 className='text-3xl font-bold text-white mb-6'>📈 Trending This Week</h2><div className='grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6'>{trending.map((movie) => (<div key={movie.id}><MovieCard movie={movie} userId={userId} onWatchlistAdd={fetchMovies} /></div>))}</div></section></div></div>);} export default HomePage;